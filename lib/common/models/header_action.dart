class HeaderAction {
  /// The widget for the icon, e.g., Icon(...) or Iconify(...).
  final String icon;

  /// The unique name for this action, which is emitted on tap.
  final String actionName;

  const HeaderAction({required this.icon, required this.actionName});
}
